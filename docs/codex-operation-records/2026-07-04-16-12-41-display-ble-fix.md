# Codex Operation Record

Timestamp: 2026-07-04 16:12:41 Asia/Shanghai

## User Goal

Confirm whether firmware was actually written to the ESP32, inspect the newly added vendor support directories, make the display show something first, and investigate why macOS cannot discover the Bluetooth keyboard.

## Important Files Read

- `Replaced files/User_Setup.h`
- `Replaced files/ST7789_Init.h`
- `Touch_calibrate/Touch_Calibrate.ino`
- `main/main.c`
- `main/esp_hid_gap.c`

## Vendor File Findings

- The vendor files target Arduino plus `TFT_eSPI` and `TFT_Touch`, not native ESP-IDF.
- They confirm the display driver is `ST7789_DRIVER`.
- Confirmed TFT pins:
  - `TFT_MISO`: GPIO12
  - `TFT_MOSI`: GPIO13
  - `TFT_SCLK`: GPIO14
  - `TFT_CS`: GPIO15
  - `TFT_DC`: GPIO2
  - `TFT_BL`: GPIO21, active high
- Confirmed touch pins match the earlier hardware image:
  - `RTP_DOUT`: GPIO39
  - `RTP_DIN`: GPIO32
  - `RTP_SCK`: GPIO25
  - `RTP_CS`: GPIO33

## Changes Made

- Added native ESP-IDF ST7789 display initialization through `esp_lcd`.
- Turned on the LCD backlight via GPIO21.
- Drew a first visible UI:
  - black background
  - top status bar
  - three large touch buttons: `C`, `V`, `X`
- Added visual feedback: touched button flashes white briefly.
- Shortened BLE device name from `CY01 Fast Keyboard` to `FastKeyboard`.
- Reduced BLE advertisement payload in `main/esp_hid_gap.c`:
  - kept official 128-bit HID service UUID
  - moved name into scan response
  - removed oversized fields from primary advertising data
- Changed BLE pairing IO capability to `ESP_IO_CAP_NONE` because this device has no keyboard entry or confirmation button.

## Verification

- Built successfully with `./scripts/build.sh`.
- Flashed successfully to `/dev/cu.usbserial-10`.
- Serial boot log confirms:
  - `lcd init ok`
  - `BLE HID started; advertising as "FastKeyboard"`
  - touch task starts with `[C] [V] [X]`
- The bad intermediate BLE advertisement build rebooted due to `ESP_ERR_INVALID_ARG`; this was fixed and overwritten with the current stable firmware.

## Open Items

- The display orientation may need visual confirmation because Codex cannot see the physical screen.
- macOS Bluetooth discovery should now search for `FastKeyboard`, not the old `CY01 Fast Keyboard`.
- If macOS still does not show it, remove any stale `CY01 Fast Keyboard` pairing entry and retry after toggling Bluetooth.
- Touch raw coordinates are visible in serial logs; calibrate after confirming the UI is visible.
