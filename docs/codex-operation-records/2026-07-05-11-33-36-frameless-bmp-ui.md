# 2026-07-05 11:33:36 Frameless BMP UI Test

## User Goal

After confirming the BMP direct-render baseline looked much better, remove the full outer frame and all button outline frames. Keep only:

- top status area
- C/V/X button labels
- three thick orange bars below the buttons

## Files Read

- `UIresources/ast_keyboard_connected.bmp`
- `UIresources/ast_keyboard_disconnected.bmp`
- `main/ui_assets.c`
- `main/ui_assets.h`

## Changes Made

- First committed the current working BMP direct-render baseline as:
  - `b3ab957 建立 BMP 直显界面基线`
- Edited both BMP source files in `UIresources/` to remove frame and border pixels.
- Preserved the top status graphics, C/V/X white labels, and thick orange bars.
- Regenerated `main/ui_assets.c` and `main/ui_assets.h` using the existing BGR RGB565 conversion plus dark-clamp/white-threshold cleanup.

## Verification

- Built successfully with `./scripts/build.sh`.
- Flashed successfully to `/dev/cu.usbserial-10`.
- Captured serial logs after flashing.
- Confirmed logs show:
  - `lcd init ok`
  - `BLE HID connected`
  - `ESP_HID_GAP: BLE GAP AUTH SUCCESS`

## Notes

- The frameless UI is flashed but not committed yet.
- It needs visual confirmation on the physical TFT before being committed as the next UI version.
