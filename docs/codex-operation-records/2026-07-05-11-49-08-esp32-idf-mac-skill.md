# 2026-07-05 11:49:08 ESP32 IDF Mac Hardware Loop Skill

## User Goal

Create a reusable Codex skill for ESP32 development in a macOS + Codex + ESP-IDF environment, abstracted from this project's development process.

## Files Created Outside This Repo

- `/Users/ky/.codex/skills/esp32-idf-mac-hardware-loop/SKILL.md`
- `/Users/ky/.codex/skills/esp32-idf-mac-hardware-loop/scripts/bmp_to_rgb565.py`
- `/Users/ky/.codex/skills/esp32-idf-mac-hardware-loop/agents/openai.yaml`

## Skill Focus

- Inspect ESP-IDF project structure before edits.
- Use small hardware-facing iterations.
- Build, flash, and capture serial logs on macOS.
- Preserve verified BLE/touch/GPIO behavior while iterating on displays or other peripherals.
- Use user visual feedback when Codex cannot directly see the hardware.
- Handle TFT display issues such as orientation, RGB/BGR order, RGB565 byte order, inversion, gamma, and low-DPI UI constraints.
- Convert static BMP UI assets to RGB565/BGR565 C arrays when static full-screen rendering is more reliable than LVGL.

## Validation

- The official skill validation script could not run because the current Python environment lacks the `yaml` module.
- Performed replacement checks:
  - verified `SKILL.md` frontmatter contains the required `name` and `description`
  - verified skill file structure exists
  - executed `scripts/bmp_to_rgb565.py` against `UIresources/ast_keyboard_connected.bmp`
  - confirmed it generated a valid C file at `/tmp/test_ui_asset.c`

## Notes

- The new skill is installed under `~/.codex/skills`, so future Codex sessions can discover it.
- This project repository was left clean before adding this operation record.
