# 2026-07-05 13:24:19 固件 v1.0 归档

## 用户目标

新建 `firmware` 目录，把已经打 tag 的 `v1.0` 固件保存到本地仓库，并上传到 GitHub。

## 已阅读的重要文件 / 信息

- `/Users/ky/.codex/skills/esp32-idf-mac-hardware-loop/SKILL.md`
- `git show -s dbef9d3`
- `/private/tmp/cy01-fw-v1.0/build/flash_args`

## 做过的修改

- 从 `dbef9d32de3c2c30e7a982c7315eb623a71e6ac0` 建立临时 worktree 并重新构建 `v1.0`。
- 新增 `firmware/v1.0/`，保存可烧录产物：
  - `cy01_esp32_fast_keyboard.bin`
  - `bootloader/bootloader.bin`
  - `partition_table/partition-table.bin`
  - `flash_args`
  - `README.md`
  - `SHA256SUMS`

## 验证

- `./scripts/build.sh` 在临时 `v1.0` worktree 中构建成功。
- 构建日志显示 app version 为 `v1.0`。
- IDF 输出的 app binary 大小为 `0xf2de0`，最小 app 分区剩余 `0x84220` 字节。
- 已生成 SHA256 校验值。

## 关键决策

- 只归档直接烧录需要的 bin、`flash_args`、说明和校验文件，不提交完整 `build/` 目录，避免仓库膨胀。
- 保持 IDF 生成的 `flash_args` 相对路径结构，因此保留 `bootloader/` 和 `partition_table/` 子目录。

## 未完成事项 / 风险 / 后续建议

- 本次没有重新烧录硬件，因为用户已经确认 `v1.0` 为稳定基线；本任务只做 tag 对应固件归档。
- 未来每个硬件确认稳定版本都建议按同样格式归档到 `firmware/vX.Y/`。
